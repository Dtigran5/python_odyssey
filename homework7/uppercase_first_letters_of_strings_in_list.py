strings_list = ['apple', 'orange', 'cherry', 'date']

uppercase_first_letters = [string[0].upper() for string in strings_list]

# Print the uppercase first letters
for letter in uppercase_first_letters:
    print(letter)
